# Copia este archivo como `.env` y completa los valores reales.

# Supabase
SUPABASE_URL=https://TU-PROYECTO.supabase.co
SUPABASE_ANON_KEY=TU_ANON_KEY

# IA (Gemini)
# Usa la misma llave en ambas variables para compatibilidad con el código actual.
GOOGLE_API_KEY=TU_LLAVE_DE_GEMINI
GEMINI_API_KEY=TU_LLAVE_DE_GEMINI

# Modelo por defecto (puedes sobreescribirlo desde la DB con get_public_ai_settings)
GEMINI_MODEL=gemini-1.5-flash

# Auth (ejemplo, usado en reset password)
RESET_REDIRECT_URL=https://tuapp.com/auth/callback

# Comentarios:
# - Asegúrate de incluir `.env` en pubspec.yaml (ya está configurado).
# - En producción, evita commitear `.env`; usa variables seguras y Edge Functions.